import React from "react"
import { View } from "react-native"


const RouterPage = (props) => {

    if (props.screenProps == "AnsverResult") {
        return (<View></View>)
    }
    return (<View></View>)
}

export default RouterPage